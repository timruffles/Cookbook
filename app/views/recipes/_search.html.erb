%div#search
  - unless @q
    %div.prompt
      What do you want to cook up for your business?
      %span.hint Search by: title / topic / chef.
  - form_for(:search, :method => :get) do
    = text_field_tag :q, @q || ''
    %button.large-b.scheme-2.awesome{:type => "submit"} Find recipes